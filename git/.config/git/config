
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true

[user]
	name = Henrik Schumacher
	email = 110597078+henrik0804@users.noreply.github.com

[includeIf "gitdir:~/code/web/"]
	path = ~/.config/git/work


[core]
	excludesFile = ~/.config/git/ignore
	autocrlf = input
	compression = 9
	whitespace = error
	preloadindex = true

[http]
	postBuffer = 15728640

[url "git@github.com:henrik0804/"]
	insteadOf = "home:"

[url "git@github.com:cre8-it/"]
	insteadOf = "c8:"


[status]
	branch = true
	showStash = true
	showUntrackedFiles = all

[diff]
	context = 3
	renames = copies
	interHunkContext = 10

[pager]
	diff = diff-so-fancy | less --tabs=4 -RFX

[diff-so-fancy]
	markEmptyLines = false

[color "diff"]
	meta = black bold
	frag = magenta
	context = white
	whitespace = yellow reverse
	old = red

[interactive]
	diffFilter = diff-so-fancy --patch
	singlekey = true


[color]
	ui = auto

[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green

[color "status"]
	added = green
	changed = yellow
	untracked = red
[format]
	pretty = format:%C(auto)%h %C(dim)%ad %C(reset)%s %C(blue)[%an]%C(reset)
[log]
	date = short
[coderabbit]
	machineId = cli/147125a403b84053a8c7e60d7c912ad6
